<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Photo library access (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android 14+ partial access ("Select photos"). Granted instead of
         READ_MEDIA_IMAGES when the user limits the app to a selection. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Photo library access (Android 12 and below) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Background indexing notification (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service for face indexing -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- allowBackup is intentionally false: the Room database holds face
         embeddings (biometric-derived vectors) and user-assigned person
         names. Android Auto Backup would otherwise copy that database to
         the user's Google Drive/cloud account, contradicting this app's
         core "stays on this device" claim. dataExtractionRules and
         fullBackupContent below are defence in depth — both rule sets
         exclude the database and DataStore preferences outright, so even
         if allowBackup is ever flipped back to true by mistake, the
         sensitive data still cannot leave the device via backup/transfer. -->
    <application
        android:name=".FaceAlbumApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaceAlbum">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FaceAlbum">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager foreground service for face indexing.
             Type "dataSync" is the Android 14 category for local media indexing. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
