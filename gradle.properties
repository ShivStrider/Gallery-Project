org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# SHA-256 of app/src/main/assets/mobile_face_net.tflite, checked by the
# verifyFaceModelPresent Gradle task before release builds. The model is now
# committed, so this is pinned to the checksum of the file actually in the
# tree — see app/src/main/assets/README_MODEL.txt for its provenance and the
# exact conversion command. Emptying this value downgrades the check to
# presence-only with a warning; -PfaceModelSha256=<hex> overrides per build.
# Re-converting from source with a different TensorFlow version will produce
# a different digest, so update this if you regenerate the asset.
faceModelSha256=72b5c2921d4fd4be3743dae54451ef2f0c13924ae9c048926152176383d657bf
